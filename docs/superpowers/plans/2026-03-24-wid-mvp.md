# wid MVP Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rust 製の `wid` CLI を立ち上げ、単一のグローバル Markdown ログに対して `wid` の全件表示と `wid now` の追記を行える MVP を実装する。

**Architecture:** `wid` は CLI 層、コマンド層、ログ操作層に分ける。保存の正本は `~/.local/share/wid/log.md` とし、`wid now` は append-only で当日セクションへ追記、`wid` は Markdown を読み取って表示する。対話入力は `wid now` 引数なし時の単一行プロンプトで扱う。

**Tech Stack:** Rust, `clap`, `anyhow`, 標準入出力, `cargo test`

---

## ファイル構成

この計画で作成・更新する主要ファイルと役割は以下のとおりです。

- `Cargo.toml`
  Rust クレート定義と依存設定
- `src/main.rs`
  CLI 実行入口
- `src/cli.rs`
  `wid` / `wid now` の引数定義とルーティング
- `src/commands/show.rs`
  デフォルト動作としてログ全件を表示
- `src/commands/done.rs`
  最後の未完了項目に `@done(...)` を付ける
- `src/commands/rm.rs`
  対話選択した 1 項目を確認後に削除する
- `src/interactive/`
  `wid done -i` / `wid rm -i` 用の最小選択 UI
- `src/commands/now.rs`
  引数あり追記と対話入力 fallback
- `src/log/model.rs`
  `DaySection` と `Entry` の基本構造
- `src/log/paths.rs`
  グローバルログパスの解決
- `src/log/format.rs`
  Markdown への整形
- `src/log/parser.rs`
  既存ログの読み取り
- `src/log/store.rs`
  読み込み、初回作成、当日セクションへの追記
- `tests/cli_now.rs`
  `wid now` の主要ふるまい検証
- `tests/cli_show.rs`
  `wid` 全件表示の検証
- `tests/parser.rs`
  Markdown パーサの検証

## 実装方針メモ

- 初期版では YAGNI を徹底し、metadata や config は入れない
- `wid now` は shell 上で空白を含む入力をそのまま受けられるように、残り引数を空白連結する
- `wid now` 引数なし時は 1 行入力だけサポートする
- ログ既存内容はできるだけ再整形せず、必要最小限の追記だけ行う
- テストでは本物のホームディレクトリを書き換えないよう、ログパス解決に差し替えポイントを持たせる
- `wid done -i` では full-screen TUI を避け、最小の 1 カラム選択 UI だけ実装する
- `wid rm -i` では選択後に `y/N` 確認を 1 段だけ入れ、安全側に倒す

### Task 1: リポジトリと Rust クレートの最小骨格を作る

**Files:**
- Create: `Cargo.toml`
- Create: `src/main.rs`
- Create: `src/cli.rs`
- Create: `src/commands/mod.rs`
- Create: `src/commands/show.rs`
- Create: `src/commands/now.rs`
- Create: `src/log/mod.rs`
- Create: `src/log/model.rs`
- Create: `src/log/paths.rs`
- Create: `src/log/format.rs`
- Create: `src/log/parser.rs`
- Create: `src/log/store.rs`

- [ ] **Step 1: `Cargo.toml` を作成する**

依存は最小限にする。

```toml
[package]
name = "wid"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
```

- [ ] **Step 2: `src/main.rs` に最小エントリポイントを書く**

```rust
mod cli;
mod commands;
mod log;

fn main() -> anyhow::Result<()> {
    cli::run()
}
```

- [ ] **Step 3: `src/cli.rs` に CLI の骨格を書く**

要件:
- 引数なし `wid` は show 動作
- `wid now [TEXT...]` を受け取る

```rust
use clap::{Parser, Subcommand};

#[derive(Parser)]
#[command(name = "wid")]
pub struct Cli {
    #[command(subcommand)]
    pub command: Option<Commands>,
}

#[derive(Subcommand)]
pub enum Commands {
    Now {
        text: Vec<String>,
    },
}
```

- [ ] **Step 4: `src/commands` と `src/log` の `mod.rs` を作る**

```rust
pub mod now;
pub mod show;
```

```rust
pub mod format;
pub mod model;
pub mod parser;
pub mod paths;
pub mod store;
```

- [ ] **Step 5: 仮実装で `cargo test` が通る状態を作る**

各関数は `todo!()` ではなく、呼ばれない範囲の最小実装にする。

- [ ] **Step 6: 骨格のビルドを確認する**

Run: `cargo test`
Expected: テスト 0 件でもよいのでコンパイル成功

- [ ] **Step 7: コミットする**

```bash
git add Cargo.toml src
git commit -m "chore: scaffold wid rust cli"
```

### Task 2: ログパス解決と append 用モデルを TDD で実装する

**Files:**
- Modify: `src/log/model.rs`
- Modify: `src/log/paths.rs`
- Modify: `src/log/format.rs`
- Test: `tests/parser.rs`

- [ ] **Step 1: ログモデル用の失敗テストを書く**

必要な最小モデル:
- `Entry { time: String, summary: String }`
- `DaySection { date: String, entries: Vec<Entry> }`

```rust
#[test]
fn format_entry_renders_bullet_line() {
    let entry = Entry {
        time: "11:32".into(),
        summary: "CI が落ちていたので修正".into(),
    };
    assert_eq!(format_entry(&entry), "- 11:32 CI が落ちていたので修正");
}
```

- [ ] **Step 2: 失敗を確認する**

Run: `cargo test format_entry_renders_bullet_line`
Expected: 未実装で FAIL

- [ ] **Step 3: モデルと formatter の最小実装を書く**

```rust
pub struct Entry {
    pub time: String,
    pub summary: String,
}

pub fn format_entry(entry: &Entry) -> String {
    format!("- {} {}", entry.time, entry.summary)
}
```

- [ ] **Step 4: ログパス解決の失敗テストを書く**

パス解決は後で差し替えやすくする。

```rust
#[test]
fn default_log_path_uses_local_share_wid_log_md() {
    let home = std::path::PathBuf::from("/tmp/example-home");
    let path = default_log_path_from_home(&home);
    assert_eq!(path, home.join(".local/share/wid/log.md"));
}
```

- [ ] **Step 5: 失敗を確認する**

Run: `cargo test default_log_path_uses_local_share_wid_log_md`
Expected: 未実装で FAIL

- [ ] **Step 6: `paths.rs` の最小実装を書く**

要件:
- `default_log_path_from_home(&Path) -> PathBuf`
- 実運用用に `default_log_path() -> Result<PathBuf>`

- [ ] **Step 7: テストを再実行する**

Run: `cargo test`
Expected: 追加したテストが PASS

- [ ] **Step 8: コミットする**

```bash
git add src/log tests/parser.rs
git commit -m "feat: add wid log model and path resolution"
```

### Task 3: Markdown パーサで日付セクションと箇条書きを読めるようにする

**Files:**
- Modify: `src/log/parser.rs`
- Modify: `src/log/model.rs`
- Test: `tests/parser.rs`

- [ ] **Step 1: 日付セクションを読む失敗テストを書く**

```rust
#[test]
fn parse_markdown_sections_and_entries() {
    let input = "# wid log\n\n## 2026-03-24\n\n- 11:32 CI が落ちていたので修正\n- 12:10 実装方針を見直した\n";
    let doc = parse_log(input).unwrap();
    assert_eq!(doc.days.len(), 1);
    assert_eq!(doc.days[0].date, "2026-03-24");
    assert_eq!(doc.days[0].entries.len(), 2);
    assert_eq!(doc.days[0].entries[1].summary, "実装方針を見直した");
}
```

- [ ] **Step 2: 失敗を確認する**

Run: `cargo test parse_markdown_sections_and_entries`
Expected: FAIL

- [ ] **Step 3: 最小パーサを実装する**

対象にする構文はこの 2 つだけ:
- `## YYYY-MM-DD`
- `- HH:MM summary`

余分な行は最初は読み飛ばしてよい。

- [ ] **Step 4: 空ログやヘッダのみのケースもテストに追加する**

```rust
#[test]
fn parse_empty_log_returns_zero_days() {
    let doc = parse_log("# wid log\n").unwrap();
    assert!(doc.days.is_empty());
}
```

- [ ] **Step 5: テストを再実行する**

Run: `cargo test --test parser`
Expected: PASS

- [ ] **Step 6: コミットする**

```bash
git add src/log/parser.rs src/log/model.rs tests/parser.rs
git commit -m "feat: parse wid markdown log"
```

### Task 4: ストア層でファイル作成と当日セクションへの追記を実装する

**Files:**
- Modify: `src/log/store.rs`
- Modify: `src/log/format.rs`
- Modify: `src/log/model.rs`
- Test: `tests/cli_now.rs`

- [ ] **Step 1: 初回作成の失敗テストを書く**

```rust
#[test]
fn append_creates_new_log_file_with_header_and_today_section() {
    // tempdir 内の空パスに append し、
    // "# wid log" と当日セクションとエントリが作られることを確認する
}
```

- [ ] **Step 2: 同日追記の失敗テストを書く**

```rust
#[test]
fn append_reuses_existing_today_section() {
    // 既に同日セクションがあるファイルに追記し、
    // 同じ見出し配下に 2 行目が足されることを確認する
}
```

- [ ] **Step 3: 失敗を確認する**

Run: `cargo test --test cli_now`
Expected: FAIL

- [ ] **Step 4: `store.rs` に append-only の最小実装を書く**

要件:
- 親ディレクトリがなければ作る
- ファイルがなければ `# wid log` から作る
- 当日見出しがある場合はそのセクション末尾へ追記
- 当日見出しがない場合は末尾に新規セクションを足す

- [ ] **Step 5: Markdown 断片を組み立てる formatter を実装する**

必要な関数例:
- `render_header()`
- `render_day_heading(date: &str)`
- `format_entry(entry: &Entry)`

- [ ] **Step 6: テストを再実行する**

Run: `cargo test --test cli_now`
Expected: PASS

- [ ] **Step 7: コミットする**

```bash
git add src/log/store.rs src/log/format.rs src/log/model.rs tests/cli_now.rs
git commit -m "feat: append entries to wid markdown log"
```

### Task 5: `wid now` コマンドの引数連結と対話入力 fallback を実装する

**Files:**
- Modify: `src/cli.rs`
- Modify: `src/commands/now.rs`
- Modify: `src/log/store.rs`
- Test: `tests/cli_now.rs`

- [ ] **Step 1: 引数連結の失敗テストを書く**

```rust
#[test]
fn now_command_joins_remaining_args_with_spaces() {
    // ["CI", "が", "落ちていたので修正"] を受けると
    // "CI が 落ちていたので修正" として保存されることを確認する
}
```

- [ ] **Step 2: 対話入力 fallback の失敗テストを書く**

コマンド関数を I/O 抽象越しに呼べるようにして、
引数なし時に入力文字列 1 行を保存することを確認する。

```rust
#[test]
fn now_command_reads_single_line_when_no_args_are_given() {
    // fake input を渡し、1 行が保存されることを確認する
}
```

- [ ] **Step 3: 失敗を確認する**

Run: `cargo test --test cli_now`
Expected: FAIL

- [ ] **Step 4: `now.rs` に最小実装を書く**

要件:
- `text` が空でなければ空白連結して保存
- `text` が空ならプロンプトを出して 1 行読む
- 空文字はエラー
- `Ctrl-C` 相当は `Result` エラーで呼び出し元へ返す

- [ ] **Step 5: `cli.rs` から `now` コマンドを接続する**

`wid now [TEXT...]` が `commands::now` を呼ぶようにする。

- [ ] **Step 6: テストを再実行する**

Run: `cargo test --test cli_now`
Expected: PASS

- [ ] **Step 7: コミットする**

```bash
git add src/cli.rs src/commands/now.rs tests/cli_now.rs
git commit -m "feat: add wid now command"
```

### Task 6: `wid` デフォルト動作として全件表示を実装する

**Files:**
- Modify: `src/cli.rs`
- Modify: `src/commands/show.rs`
- Modify: `src/log/parser.rs`
- Test: `tests/cli_show.rs`

- [ ] **Step 1: 全件表示の失敗テストを書く**

```rust
#[test]
fn default_command_prints_all_log_entries() {
    // 2 日分のログを用意し、
    // `wid` 実行相当で両方のエントリが出力されることを確認する
}
```

- [ ] **Step 2: 失敗を確認する**

Run: `cargo test --test cli_show`
Expected: FAIL

- [ ] **Step 3: `show.rs` の最小実装を書く**

初期表示は凝った整形をせず、ファイル内容をそのまま出すか、
パース結果をほぼ同じ見た目で再出力する。まずは壊れにくい方を選ぶ。

- [ ] **Step 4: `cli.rs` のデフォルト動作を接続する**

要件:
- subcommand なしなら `show` を呼ぶ

- [ ] **Step 5: テストを再実行する**

Run: `cargo test --test cli_show`
Expected: PASS

- [ ] **Step 6: コミットする**

```bash
git add src/cli.rs src/commands/show.rs tests/cli_show.rs
git commit -m "feat: show wid log by default"
```

### Task 7: 仕上げとして README と基本検証を追加する

**Files:**
- Create: `README.md`
- Modify: `Cargo.toml`
- Test: `tests/cli_now.rs`
- Test: `tests/cli_show.rs`
- Test: `tests/parser.rs`

- [ ] **Step 1: README の失敗観点を整理する**

README に最低限含める内容:
- `wid` の目的
- `wid`
- `wid now some text`
- `wid now`
- 保存場所

- [ ] **Step 2: `README.md` を書く**

最低限の利用例を含める。

```md
# wid

`wid` は "what I'm doing" を記録するための CLI です。

## Usage

```bash
wid
wid now CI が落ちていたので修正
wid now
```
```

- [ ] **Step 3: 全テストを実行する**

Run: `cargo test`
Expected: PASS

- [ ] **Step 4: 実運用コマンドを手で確認する**

Run: `cargo run -- now first entry`
Expected: `~/.local/share/wid/log.md` に 1 件追記される

Run: `cargo run`
Expected: 追記したログ全体が表示される

- [ ] **Step 5: 最終コミットを作る**

```bash
git add README.md Cargo.toml src tests
git commit -m "feat: implement wid markdown logging mvp"
```

## 完了チェック

- [ ] `wid` が全件表示する
- [ ] `wid now [TEXT...]` が 1 件追記する
- [ ] `wid now` が対話入力で 1 件追記する
- [ ] ログファイルを自動作成する
- [ ] Markdown 形式が維持される
- [ ] `cargo test` が通る

### Task 8: `wid done` の最小完了コマンドを追加する

**Files:**
- Create: `src/commands/done.rs`
- Modify: `src/commands/mod.rs`
- Modify: `src/cli.rs`
- Modify: `src/log/store.rs`
- Test: `tests/cli_done.rs`

- [ ] **Step 1: `wid done` の失敗テストを書く**

最低限必要なケース:
- 最後の未完了項目を完了にする
- 最後がすでに完了なら、その前の未完了項目を完了にする
- 未完了項目がなければエラー

例:

```rust
#[test]
fn done_command_marks_last_unfinished_entry() {
    // temp HOME にログを用意して `wid done` を実行し、
    // 最後の未完了行の末尾に @done(YYYY-MM-DD HH:MM) が付くことを確認する
}
```

- [ ] **Step 2: 失敗を確認する**

Run: `cargo test --test cli_done`
Expected: FAIL

- [ ] **Step 3: ストア層に最小更新ロジックを書く**

要件:
- ログ全体から最後の未完了項目を見つける
- 対象 1 行の末尾に ` @done(YYYY-MM-DD HH:MM)` を追加する
- 既存テキストをできるだけ保持する

- [ ] **Step 4: `src/commands/done.rs` を実装する**

要件:
- 引数なし
- 現在ローカル時刻から `YYYY-MM-DD HH:MM` を作る
- 最後の未完了項目へ完了タグを付ける
- 未完了項目がなければエラー

- [ ] **Step 5: CLI に `done` を接続する**

`wid done` を追加する。

- [ ] **Step 6: テストを再実行する**

Run: `cargo test --test cli_done`
Expected: PASS

Run: `cargo test`
Expected: PASS

- [ ] **Step 7: README も必要なら 1 行だけ更新する**

`wid done` を usage に追加する。

- [ ] **Step 8: コミットする**

```bash
git add README.md src tests
git commit -m "feat: add wid done command"
```

### Task 9: `wid done -i` の最小インタラクティブ選択を追加する

**Files:**
- Modify: `Cargo.toml`
- Modify: `src/cli.rs`
- Modify: `src/commands/done.rs`
- Create: `src/interactive/mod.rs`
- Create: `src/interactive/done_picker.rs`
- Modify: `src/log/model.rs`
- Modify: `src/log/parser.rs`
- Modify: `src/log/store.rs`
- Test: `tests/cli_done.rs`

- [ ] **Step 1: `wid done -i` の失敗テストを書く**

最低限必要なケース:
- `wid done -i` が未完了項目を新しい順で選択候補にする
- `Enter` で選択中の 1 件だけが完了になる
- `q` または `Esc` でキャンセルしてログを変更しない

例:

```rust
#[test]
fn done_interactive_marks_selected_unfinished_entry() {
    // 2 件以上の未完了項目を持つログを用意し、
    // 対話入力で下移動 + Enter を送って狙った 1 件だけが @done になることを確認する
}
```

- [ ] **Step 2: 失敗を確認する**

Run: `cargo test --test cli_done`
Expected: FAIL

- [ ] **Step 3: 未完了項目の内部モデルを追加する**

要件:
- `YYYY-MM-DD`, `HH:MM`, `summary` を持つ選択表示用の値を定義する
- 実ファイル更新に必要な位置情報も保持できるようにする
- 通常の `wid done` と `wid done -i` が同じ対象解決ロジックを共有できる形にする

- [ ] **Step 4: ストア層に未完了一覧取得を実装する**

要件:
- 全ログから `@done(...)` のない項目だけを抽出する
- 新しい順で返す
- day section 外の行は含めない
- 選択された項目を 1 件だけ `@done(...)` に更新できるようにする

- [ ] **Step 5: `done_picker.rs` に最小選択 UI を実装する**

要件:
- 依存は `crossterm` のような最小限に留める
- 1 カラム一覧だけを表示する
- `j` / `Down` で次、`k` / `Up` で前
- `Enter` で確定
- `q` / `Esc` でキャンセル
- 終了時に画面状態をできるだけ元へ戻す

- [ ] **Step 6: `src/commands/done.rs` に `-i` 分岐を追加する**

要件:
- デフォルトの `wid done` はこれまで通り最後の未完了項目を完了
- `wid done -i` のときだけ picker を開く
- 未完了項目が 0 件なら picker を開かずエラー
- キャンセルは成功終了にするか、明示的な中断メッセージを出して非更新にする

- [ ] **Step 7: テストを再実行する**

Run: `cargo test --test cli_done`
Expected: PASS

Run: `cargo test`
Expected: PASS

- [ ] **Step 8: README を最小更新する**

`wid done -i` を usage に追加する。

- [ ] **Step 9: コミットする**

```bash
git add Cargo.toml Cargo.lock README.md src tests
git commit -m "feat: add interactive done picker"
```

### Task 10: `wid rm -i` の最小インタラクティブ削除を追加する

**Files:**
- Modify: `src/cli.rs`
- Create: `src/commands/rm.rs`
- Modify: `src/commands/mod.rs`
- Modify: `src/interactive/done_picker.rs`
- Modify: `src/log/model.rs`
- Modify: `src/log/store.rs`
- Modify: `README.md`
- Test: `tests/cli_done.rs`
- Create: `tests/cli_rm.rs`

- [ ] **Step 1: `wid rm -i` の失敗テストを書く**

最低限必要なケース:
- 全項目を新しい順で選択対象にする
- `Enter` 後に `y` を押したときだけ 1 件削除する
- `n`、`q`、`Esc`、その他のキーでは削除しない
- 項目が 0 件ならエラーにする

例:

```rust
#[test]
fn rm_interactive_deletes_selected_entry_after_confirmation() {
    // 完了済みを含む複数項目のログを用意し、
    // 選択 + confirm で対象 1 行だけ消えることを確認する
}
```

- [ ] **Step 2: 失敗を確認する**

Run: `cargo test --test cli_rm`
Expected: FAIL

- [ ] **Step 3: 全項目の内部モデルを追加する**

要件:
- 完了済みかどうかを保持できる選択表示用の値を定義する
- 更新対象の行位置と stale-target 判定に必要な識別情報を持てるようにする
- `done` 用の未完了項目モデルと責務が重なるなら整理する

- [ ] **Step 4: ストア層に全項目収集と 1 行削除を実装する**

要件:
- 全ログから全項目を新しい順で表示できる材料を返す
- `@done(...)` の有無を問わず 1 行を対象にできる
- 選択中にファイルが変わった場合は stale target として弾く
- 削除は対象 1 行だけに限定し、保存形式は崩さない
- 空になった日付見出しの自動削除はまだ行わない

- [ ] **Step 5: 対話 UI に削除確認を追加する**

要件:
- 既存の一覧 picker を再利用できる形を優先する
- `Enter` で確認に進み、`y` でだけ確定する
- `n`、`q`、`Esc`、その他のキーでは削除しない
- 確認をキャンセルしたら一覧へ戻るか、安全に終了する

- [ ] **Step 6: `src/commands/rm.rs` と CLI 接続を書く**

要件:
- まずは `wid rm -i` だけサポートする
- 非対話の `wid rm` はまだ実装しないか、未対応として扱う
- 項目がなければ picker を開かずエラー

- [ ] **Step 7: テストを再実行する**

Run: `cargo test --test cli_rm`
Expected: PASS

Run: `cargo test`
Expected: PASS

- [ ] **Step 8: README を最小更新する**

`wid rm -i` を usage に追加する。

- [ ] **Step 9: コミットする**

```bash
git add README.md src tests
git commit -m "feat: add interactive wid rm command"
```
